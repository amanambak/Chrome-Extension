---
phase: 04-ui-panel-page-resize
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [transcript-panel.js, content.js]
autonomous: true
requirements: [UI-01, UI-02, UI-04]

must_haves:
  truths:
    - "Panel is fixed at 350px on the right side of the viewport"
    - "Showing the panel applies a 350px right margin to the html element"
    - "Hiding the panel removes the right margin from the html element"
  artifacts:
    - path: "transcript-panel.js"
      provides: "Updated styles and visibility hooks"
    - path: "content.js"
      provides: "LayoutManager for page shifting"
  key_links:
    - from: "transcript-panel.js"
      to: "content.js"
      via: "visibility change callback or event"
---

<objective>
Implement the core layout shifting mechanism that moves page content to the left when the transcript panel is visible.

Purpose: Prevents the extension panel from overlapping with the host page content, ensuring a "natural" integrated feel.
Output: Updated transcript-panel.js with improved styles and visibility hooks; updated content.js with html margin management.
</objective>

<execution_context>
@C:/Users/amanp/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/amanp/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-panel-page-resize/04-RESEARCH.md
@transcript-panel.js
@content.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TranscriptPanel for Visibility Lifecycle</name>
  <files>transcript-panel.js</files>
  <action>
    - Refine the Shadow DOM styles for `#panel-root`: ensure `position: fixed`, `right: 0`, `top: 0`, `width: 350px`, `height: 100vh`, and `z-index: 2147483647`.
    - Update the `TranscriptPanel` class to accept an optional `onVisibilityChange` callback in the constructor.
    - Call this callback in `show()` and `hide()` methods, passing the new visibility state (true/false).
    - Ensure the host element `#audio-ai-panel-host` has `display: block` but does not interfere with host layout when panel is hidden.
  </action>
  <verify>
    <automated>grep "onVisibilityChange" transcript-panel.js</automated>
  </verify>
  <done>TranscriptPanel notifies of visibility changes and has correct fixed positioning CSS.</done>
</task>

<task type="auto">
  <name>Task 2: Implement LayoutManager in content.js</name>
  <files>content.js</files>
  <action>
    - Create a `LayoutManager` object or class in `content.js`.
    - Implement `updateLayout(isVisible)` method:
      - If `isVisible`, set `document.documentElement.style.marginRight = '350px'`.
      - If `!isVisible`, set `document.documentElement.style.marginRight = '0px'`.
      - Add a smooth transition to `document.documentElement.style.transition = 'margin-right 0.3s ease-in-out'`.
    - Ensure it safely handles cases where `document.documentElement` might be missing (though rare in content scripts).
  </action>
  <verify>
    <automated>grep "marginRight" content.js</automated>
  </verify>
  <done>LayoutManager can toggle the html element's margin.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Panel Toggle to LayoutManager</name>
  <files>content.js</files>
  <action>
    - Update `getPanel()` in `content.js` to initialize the `TranscriptPanel` with a callback that calls `LayoutManager.updateLayout`.
    - Update the `SHOW_PANEL` message listener and the `TRANSCRIPT_UPDATE` listener (which calls `p.show()`) to ensure the layout is updated whenever the panel visibility changes.
    - Add a `HIDE_PANEL` message listener (for future use/completeness) that hides the panel and resets layout.
  </action>
  <verify>
    <automated>grep "updateLayout" content.js</automated>
  </verify>
  <done>Panel visibility and page margin are synchronized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Panel → Host DOM | The extension modifies the host page's root layout (margin) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | html styles | mitigate | Only modify `marginRight` and `transition`. Avoid overwriting unrelated styles. |
| T-04-01-02 | Denial of Service | Layout Reflow | mitigate | Use CSS transitions for smoothness and avoid synchronous layout thrashing. |
</threat_model>

<verification>
1. Load extension.
2. Trigger panel show (e.g. by starting capture or sending SHOW_PANEL).
3. Verify `<html>` has `margin-right: 350px`.
4. Verify panel is visible on the right.
5. Manually hide panel (if possible) or send `HIDE_PANEL` and verify margin is removed.
</verification>

<success_criteria>
- Panel is 350px wide and fixed right.
- Page content shifts left via `html` margin when panel is shown.
- Transition is smooth.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-panel-page-resize/04-01-SUMMARY.md`
</output>
