---
phase: 04-ui-panel-page-resize
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified: [transcript-panel.js, content.js]
autonomous: false
requirements: [UI-03, UI-04]

must_haves:
  truths:
    - "Panel content scrolls correctly when taller than the viewport"
    - "Showing/hiding the panel triggers a window.resize event on the host page"
    - "YouTube video player resizes correctly to fit the shifted page content"
  artifacts:
    - path: "transcript-panel.js"
      provides: "Responsive CSS for panel content"
    - path: "content.js"
      provides: "window.resize event dispatch"
  key_links:
    - from: "content.js"
      to: "host page"
      via: "window.dispatchEvent(new Event('resize'))"
---

<objective>
Finalize the responsive behavior and ensure the extension works correctly on major sites by forcing layout recalculations.

Purpose: Fixes specific site behaviors where the video player (YouTube/Meet) doesn't automatically resize to fit the new margin-shifted area.
Output: Updated panel styles and layout event dispatching.
</objective>

<execution_context>
@C:/Users/amanp/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/amanp/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-panel-page-resize/04-RESEARCH.md
@transcript-panel.js
@content.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch window.resize on Panel Toggle</name>
  <files>content.js</files>
  <action>
    - Update the `LayoutManager.updateLayout` in `content.js` to dispatch a `window.resize` event after applying or removing the `html` margin.
    - Use `window.dispatchEvent(new Event('resize'))`.
    - Consider wrapping this in a short `setTimeout` (e.g. 300ms) to allow the CSS transition to complete before triggering the JS reflow.
  </action>
  <verify>
    <automated>grep "dispatchEvent" content.js</automated>
  </verify>
  <done>Host pages receive a resize event to trigger layout recalculation.</done>
</task>

<task type="auto">
  <name>Task 2: Refine Panel Responsiveness & Z-Index</name>
  <files>transcript-panel.js</files>
  <action>
    - Update `transcript-panel.js` Shadow styles:
      - Set `max-height: 100vh` on `#panel-root`.
      - Ensure `#transcript-container` has `overflow-y: auto` and fills the available height.
      - Verify the header is fixed and the scroll area is clearly defined.
      - Confirm `z-index: 2147483647` (max allowed) is applied to the panel root.
  </action>
  <verify>
    <automated>grep "overflow-y" transcript-panel.js</automated>
  </verify>
  <done>Panel content scales and scrolls reliably across window sizes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full Phase 4 layout and page-shifting implementation</what-built>
  <how-to-verify>
    - Visit [YouTube](https://www.youtube.com).
    - Show the panel (trigger capture or use `SHOW_PANEL` message).
    - 1. Verify the YouTube header shifts left and doesn't overlap.
    - 2. Verify the YouTube video player resizes automatically to fit the new width (no video is cut off).
    - 3. Verify the extension panel is scrollable as cards accumulate.
    - 4. Verify the panel is on top of all YouTube elements.
    - 5. Toggle panel off and verify host page restores perfectly.
    - Repeat similar checks on [Google Meet](https://meet.google.com) if possible.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Content Script → Host Window | The extension triggers global window events |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Denial of Service | Event Loop | mitigate | Only dispatch resize event once per toggle, not repeatedly. |
</threat_model>

<verification>
1. Open YouTube.
2. Toggle panel.
3. Observe player resizing and header shifting.
4. Verify no overlapping content.
</verification>

<success_criteria>
- YouTube video player resizes correctly.
- Panel is scrollable and has max z-index.
- Toggle lifecycle is complete and clean.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-panel-page-resize/04-03-SUMMARY.md`
</output>
