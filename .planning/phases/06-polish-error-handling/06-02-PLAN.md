---
phase: 06-polish-error-handling
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [offscreen.js, background.js, content.js, transcript-panel.js]
autonomous: true
requirements: [UX-04]

must_haves:
  truths:
    - "Offscreen document detects and relays WebSocket errors"
    - "Service worker relays API errors to the content script"
    - "Content script displays ErrorCard for API failures"
  artifacts:
    - path: "offscreen.js"
      provides: "Deepgram error detection and relay"
    - path: "background.js"
      provides: "Gemini error relay and consolidated API error handling"
    - path: "content.js"
      provides: "UI update for API errors"
    - path: "transcript-panel.js"
      provides: "addErrorCard method"
---

<objective>
Implement end-to-end error handling from the background/offscreen contexts to the visible UI.

Purpose: Ensure users are informed when APIs fail instead of facing silent hangs.
Output: Integrated error relay logic in service worker, offscreen document, and content scripts.
</objective>

<execution_context>
@C:/Users/amanp/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/amanp/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-error-handling/06-RESEARCH.md
@offscreen.js
@background.js
@content.js
@transcript-panel.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deepgram Error Handling in offscreen.js</name>
  <files>offscreen.js</files>
  <action>
    - Update `socket.onclose` to detect non-1000 closure codes.
    - Send `chrome.runtime.sendMessage({ type: 'API_ERROR', source: 'Deepgram', message: '...' })` when disconnect occurs.
    - (Optional) Implement a single retry attempt if the connection closes prematurely.
  </action>
  <verify>
    <automated>grep -q "API_ERROR" offscreen.js</automated>
  </verify>
  <done>Deepgram WebSocket errors are now relayed to the extension.</done>
</task>

<task type="auto">
  <name>Task 2: Error Relay in background.js</name>
  <files>background.js</files>
  <action>
    - Update `onMessage` listener to catch `API_ERROR` and relay it to the `targetTabId` (or last active tab).
    - Ensure Gemini fetch errors (already partially handled) correctly use the `AI_RESPONSE_ERROR` type or consolidated `API_ERROR` if appropriate.
  </action>
  <verify>
    <automated>grep -q "API_ERROR" background.js</automated>
  </verify>
  <done>Service worker correctly relays background/offscreen errors to the UI.</done>
</task>

<task type="auto">
  <name>Task 3: Update content.js and transcript-panel.js for Error UI</name>
  <files>content.js, transcript-panel.js</files>
  <action>
    - In `transcript-panel.js`: Add `addErrorCard(message, source)` method that instantiates `ErrorCard` and appends it to the container.
    - In `content.js`: Add listener for `API_ERROR` message.
    - Call `panel.addErrorCard()` when an `API_ERROR` is received.
  </action>
  <verify>
    <automated>grep -q "addErrorCard" transcript-panel.js && grep -q "API_ERROR" content.js</automated>
  </verify>
  <done>The UI now displays a distinct ErrorCard for Deepgram and other API failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deepgram/Gemini API → Extension | Externally-generated error messages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | Error message display | mitigate | Use `textContent` when displaying error messages from external sources. |
</threat_model>

<success_criteria>
1. Deepgram disconnects result in an ErrorCard in the panel.
2. Gemini API failures show either an inline error or an ErrorCard.
3. Errors are handled gracefully without crashing the extension.
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-error-handling/06-02-SUMMARY.md`
</output>
