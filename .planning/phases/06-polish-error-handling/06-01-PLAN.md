---
phase: 06-polish-error-handling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [manifest.json, copy-button.js, error-card.js]
autonomous: true
requirements: [UX-01, UX-04]

must_haves:
  truths:
    - "Extension has clipboardWrite permission"
    - "copy-button.js exists and is registered"
    - "error-card.js exists and is registered"
  artifacts:
    - path: "manifest.json"
      provides: "Permissions and content script registration"
    - path: "copy-button.js"
      provides: "Reusable copy-to-clipboard button component"
    - path: "error-card.js"
      provides: "Component for displaying prominent API errors"
  key_links:
    - from: "manifest.json"
      to: "copy-button.js"
      via: "content_scripts[0].js array"
    - from: "manifest.json"
      to: "error-card.js"
      via: "content_scripts[0].js array"
---

<objective>
Establish the foundation for polish and error handling by updating manifest permissions and creating reusable UI components.

Purpose: Provide the necessary permissions for clipboard access and the building blocks for new UI elements.
Output: Updated manifest.json, new copy-button.js, and new error-card.js.
</objective>

<execution_context>
@C:/Users/amanp/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/amanp/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-error-handling/06-RESEARCH.md
@manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update manifest.json permissions and scripts</name>
  <files>manifest.json</files>
  <action>
    Add "clipboardWrite" to the "permissions" array.
    Add "copy-button.js" and "error-card.js" to the "js" array in the first "content_scripts" entry (before "content.js").
  </action>
  <verify>
    <automated>grep -q "clipboardWrite" manifest.json && grep -q "copy-button.js" manifest.json && grep -q "error-card.js" manifest.json</automated>
  </verify>
  <done>manifest.json updated with required permission and script references.</done>
</task>

<task type="auto">
  <name>Task 2: Implement copy-button.js</name>
  <files>copy-button.js</files>
  <action>
    Create a Vanilla JS class `CopyButton`.
    - Constructor accepts the text to copy.
    - `element` property holds the button DOM element.
    - Implements `navigator.clipboard.writeText` logic with visual feedback (e.g., text changes from "Copy" to "Copied!" for 2 seconds).
    - Uses an inline SVG for the copy icon.
    - Includes basic internal styling or references CSS classes expected in the panel.
  </action>
  <verify>
    <automated>test -f copy-button.js && grep "CopyButton" copy-button.js</automated>
  </verify>
  <done>copy-button.js created with working clipboard logic and icon.</done>
</task>

<task type="auto">
  <name>Task 3: Implement error-card.js</name>
  <files>error-card.js</files>
  <action>
    Create a Vanilla JS class `ErrorCard`.
    - Constructor accepts error `message` and optional `source`.
    - `element` property holds a div with `error-card` class.
    - Displays a prominent error icon (inline SVG) and the message.
    - Designed to be added directly to the `transcript-panel.js` container.
  </action>
  <verify>
    <automated>test -f error-card.js && grep "ErrorCard" error-card.js</automated>
  </verify>
  <done>error-card.js created for displaying API-level errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser API → Clipboard | Permission-gated access to system clipboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | copy-button.js | mitigate | Use `textContent` for any labels or dynamic text to prevent XSS. |
| T-06-02 | Information Disclosure | Clipboard API | accept | User must explicitly click the button to trigger copy; only AI response text is copied. |
</threat_model>

<success_criteria>
1. manifest.json includes "clipboardWrite" permission.
2. copy-button.js and error-card.js are present in the project root.
3. Content scripts are correctly registered in manifest.json.
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-error-handling/06-01-SUMMARY.md`
</output>
