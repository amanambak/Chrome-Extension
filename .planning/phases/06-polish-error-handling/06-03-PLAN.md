---
phase: 06-polish-error-handling
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified: [ai-response-card.js, transcript-panel.js]
autonomous: false
requirements: [UX-01, UX-04]

must_haves:
  truths:
    - "AI response cards have a copy button"
    - "Clicking copy provides visual feedback"
    - "Panel has refined visual styles (animations, scrollbar)"
  artifacts:
    - path: "ai-response-card.js"
      provides: "AI response cards with copy button integration"
    - path: "transcript-panel.js"
      provides: "Polished CSS and layout"
---

<objective>
Finalize the user interface by integrating components and refining the overall visual style.

Purpose: Provide a polished, professional look and feel for the extension and complete the core UX requirements.
Output: Refined side panel with fully integrated copy functionality and improved aesthetics.
</objective>

<execution_context>
@C:/Users/amanp/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/amanp/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-error-handling/06-RESEARCH.md
@ai-response-card.js
@transcript-panel.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ai-response-card.js with CopyButton</name>
  <files>ai-response-card.js</files>
  <action>
    - Import/Reference `CopyButton` class (available globally via content script script order).
    - In `ai-response-card.js`, instantiate `CopyButton` with the finalized response text once streaming is complete (`isDone: true`).
    - Append the copy button element to the card.
    - Ensure it is styled appropriately within the card (e.g., positioned top-right).
  </action>
  <verify>
    <automated>grep -q "CopyButton" ai-response-card.js</automated>
  </verify>
  <done>AI response cards now feature a functional copy button.</done>
</task>

<task type="auto">
  <name>Task 2: Refine Panel Styles in transcript-panel.js</name>
  <files>transcript-panel.js</files>
  <action>
    - Update CSS in `transcript-panel.js`:
        - Add `.error-card` styles (distinct from utterance-card).
        - Improve `.copy-button` styles (subtle background, hover effect).
        - Refine scrollbar aesthetics and animations.
        - Ensure good spacing and readability across all cards.
        - Add a slight background transition for card additions.
  </action>
  <verify>
    <automated>grep -q "error-card" transcript-panel.js && grep -q "copy-button" transcript-panel.js</automated>
  </verify>
  <done>The panel UI is visually refined with consistent styling and improved usability.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Final Visual & Functional Verification</name>
  <files>N/A</files>
  <action>
    Verify final polished UI with copy-to-clipboard and error states:
    1. Load extension in chrome://extensions.
    2. Start capture on a YouTube video.
    3. Verify AI answers appear with a copy button.
    4. Click copy and verify the clipboard content and visual "Copied!" feedback.
    5. Simulate a Deepgram disconnect (e.g., manually close WebSocket in DevTools) and verify the ErrorCard appears.
    6. Verify general panel aesthetics.
  </action>
  <verify>approved</verify>
  <done>Final UI is verified and approved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → User | Visual presentation of data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | CSS Injection | mitigate | Styles are scoped within the Shadow DOM in transcript-panel.js. |
</threat_model>

<success_criteria>
1. All AI response cards have a working copy button.
2. The panel UI is visually polished and handles error states clearly.
3. Functional verification of success and error flows passes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-error-handling/06-03-SUMMARY.md`
</output>
