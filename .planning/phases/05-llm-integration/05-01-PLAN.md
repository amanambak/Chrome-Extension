---
phase: 05-llm-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [background.js]
autonomous: true
requirements: [AI-01, AI-02, AI-03, LLM-01, LLM-03, LLM-04]

must_haves:
  truths:
    - "Background service worker can fetch from Gemini API using keys in config.js"
    - "System prompt correctly instructs model to classify and respond in one pass"
    - "Streaming chunks from Gemini are relayed to the content script in real-time"
  artifacts:
    - path: "background.js"
      provides: "Gemini streaming infrastructure"
  key_links:
    - from: "background.js"
      to: "https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:streamGenerateContent"
      via: "fetch API"
---

<objective>
Implement the streaming Gemini API integration in the background service worker. This plan focuses on the communication infrastructure between the extension and Google's Generative AI platform.

Purpose: Enable the extension to generate AI responses for finalized utterances using Gemini 2.0 Flash.
Output: Updated background.js with streaming relay logic and system prompt classification.
</objective>

<execution_context>
@C:/Users/amanp/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/amanp/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-llm-integration/05-RESEARCH.md
@config.js
@background.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gemini API Streaming Infrastructure</name>
  <files>background.js</files>
  <action>
    - Add `importScripts('config.js')` at the top of background.js to access CONFIG.
    - Implement a listener for the `PROCESS_UTTERANCE` message containing `text`, `utteranceId`, and `tabId`.
    - Create a `streamGeminiResponse(text, utteranceId, tabId)` function.
    - Use `fetch` with the `streamGenerateContent` endpoint specified in research.
    - Implement the system prompt per research: "Classify user utterance as QUESTION or STATEMENT. If QUESTION: Provide concise answer. If STATEMENT: Provide 2-3 short suggestions. Prefix with [ANSWER] or [SUGGESTION]."
    - Use `temperature: 0.7` and `maxOutputTokens: 200` as recommended.
  </action>
  <verify>
    <automated>Check background.js for fetch call to gemini-2.0-flash with streaming endpoint.</automated>
  </verify>
  <done>Background service worker is ready to handle LLM requests.</done>
</task>

<task type="auto">
  <name>Task 2: Streaming Buffer & Relay Logic</name>
  <files>background.js</files>
  <action>
    - In `streamGeminiResponse`, process the response body using a `ReadableStream` reader.
    - Parse the incoming SSE chunks. Note: Gemini returns JSON objects representing parts of the response.
    - Extract text from `candidates[0].content.parts[0].text`.
    - Relay each chunk to the target tab using `chrome.tabs.sendMessage` with type `AI_RESPONSE_CHUNK`.
    - Payload should include: `utteranceId`, `text` (chunk), and `isDone` flag.
    - Handle errors by sending an `AI_RESPONSE_ERROR` message to the tab.
  </action>
  <verify>
    <automated>Verify background.js contains logic to read stream and relay chunks via sendMessage.</automated>
  </verify>
  <done>Relay logic successfully sends AI tokens to the content script as they arrive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service Worker -> Gemini API | Untrusted input (transcript) sent to external API |
| Gemini API -> Service Worker | Untrusted output (AI response) received from external API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Info Disclosure | background.js | mitigate | Use importScripts for config.js (gitignored) to protect API keys. |
| T-05-02 | Denial of Service | background.js | mitigate | Limit maxOutputTokens to 200 to prevent runaway streaming costs/latency. |
| T-05-03 | Tampering | background.js | mitigate | Use clear separation between System Instruction and User Content in payload. |
</threat_model>

<success_criteria>
- `background.js` successfully calls the Gemini API when triggered.
- API keys are correctly retrieved from `config.js`.
- Streaming chunks are relayed to the content script with correct metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/05-llm-integration/05-01-SUMMARY.md`
</output>
