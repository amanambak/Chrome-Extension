---
phase: 05-llm-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [ai-response-card.js, manifest.json, transcript-panel.js]
autonomous: true
requirements: [AI-02, AI-05, LLM-02]

must_haves:
  truths:
    - "AI response card has distinct styling from transcription cards (subtle background, icon)"
    - "New component correctly registered in manifest.json for content scripts"
    - "Transcript panel CSS includes styles for AI-specific elements like badges"
  artifacts:
    - path: "ai-response-card.js"
      provides: "AI response card component"
  key_links:
    - from: "manifest.json"
      to: "ai-response-card.js"
      via: "content_scripts"
---

<objective>
Implement the UI component for AI responses and register it in the extension manifest. This plan ensures that AI responses have a dedicated, styled container.

Purpose: Provide a visual container for Gemini's answers and suggestions.
Output: New ai-response-card.js component and updated manifest.json.
</objective>

<execution_context>
@C:/Users/amanp/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/amanp/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@manifest.json
@transcript-panel.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement AI Response Card Component</name>
  <files>ai-response-card.js</files>
  <action>
    - Create a class `AiResponseCard`.
    - Like UtteranceCard, it should have an `element` property.
    - Structure: A main card `div` with child elements for the classification badge (`[ANSWER]` vs `[SUGGESTION]`) and the response text content.
    - Implement `appendChunk(text)` to add streaming tokens.
    - Implement `setType(type)` to show either "Answer" or "Suggestion" badge based on classification.
    - Initial state should show a loading indicator (e.g., three pulsing dots).
  </action>
  <verify>
    <automated>Verify ai-response-card.js exports AiResponseCard with appendChunk method.</automated>
  </verify>
  <done>AI response card component logic is complete.</done>
</task>

<task type="auto">
  <name>Task 2: Register Component & Update Panel Styles</name>
  <files>manifest.json, transcript-panel.js</files>
  <action>
    - Add `ai-response-card.js` to the `content_scripts.js` array in manifest.json, BEFORE `content.js`.
    - Update the CSS in `transcript-panel.js`'s `init()` method:
      - Add `.ai-response-card` styles: subtle blue background (`#1e2a3a`), rounded corners, margins.
      - Add `.ai-badge` styles: small, uppercase, distinct colors for answers vs suggestions.
      - Add `.loading-dots` animation styles for the initial generation state.
  </action>
  <verify>
    <automated>Check manifest.json for ai-response-card.js. Check transcript-panel.js for .ai-response-card CSS.</automated>
  </verify>
  <done>AI UI components are integrated into the panel system.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component -> DOM | Direct DOM manipulation for rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Cross-Site Scripting (XSS) | ai-response-card.js | mitigate | Use textContent for appending AI chunks to prevent execution of malicious content. |
</threat_model>

<success_criteria>
- AI response card displays properly within the transcript panel.
- Badges and loading states are visually distinct.
- Script is correctly loaded in all tabs via manifest.
</success_criteria>

<output>
After completion, create `.planning/phases/05-llm-integration/05-02-SUMMARY.md`
</output>
