---
phase: 03-transcription-segmentation
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: [transcript-panel.js, content.js]
autonomous: true
requirements: [TRNS-01, TRNS-02, TRNS-03]

must_haves:
  truths:
    - "Panel auto-scrolls to show the latest utterance"
    - "Panel UI is isolated from host page styles"
  artifacts:
    - path: "transcript-panel.js"
      provides: "Auto-scroll and style isolation"
---

<objective>
Refine the transcription panel with auto-scroll logic, robust CSS isolation, and verify that segmentation works correctly across various speech patterns.

Purpose: Without auto-scroll, long sessions become unusable as text goes off-screen. Robust CSS isolation ensures the extension remains usable across different websites.
Output: A polished, scrollable transcription feed that reliably segments utterances.
</objective>

<execution_context>
@C:/Users/amanp/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/amanp/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-transcription-segmentation/03-RESEARCH.md
@.planning/phases/03-transcription-segmentation/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auto-Scroll and Layout Refinements</name>
  <files>transcript-panel.js</files>
  <action>
    - Update the `TranscriptPanel` class to include:
      - `overflow-y: auto` on the card container.
      - A `scrollToBottom()` method that ensures the latest card is visible.
      - Call `scrollToBottom()` whenever a new card is added or an active card's content updates.
    - Refine Shadow DOM styles:
      - Set a default font-family (system-ui).
      - Ensure `color-scheme: dark` is set within the shadow root.
      - Add padding and card spacing.
  </action>
  <verify>
    <automated>grep -n "scrollTop" transcript-panel.js</automated>
  </verify>
  <done>The panel automatically scrolls as new text arrives, keeping the latest words in view.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: End-to-End Transcription & Segmentation Verification</name>
  <what-built>Full Phase 3 pipeline: Relay → State Machine → UI Components.</what-built>
  <how-to-verify>
    1. Load the extension in Chrome.
    2. Open a YouTube video or Google Meet.
    3. Start audio capture.
    4. Verify:
       - Text appears in real-time (Interim).
       - Text locks in after a few words (Final).
       - Pausing for ~1.5 seconds creates a new card (UtteranceEnd).
       - Resuming speech continues in the new card.
       - Scrolling works when cards fill the panel height.
    5. Test on a page with complex styles (e.g., Gmail or Notion) to ensure Shadow DOM isolation holds.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI Rendering | Styles must not leak from or to host page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Denial of Service | transcript-panel.js | mitigate | Limit maximum number of cards in DOM if memory becomes an issue (Deferred to Phase 6). |
</threat_model>

<verification>
- Start capture.
- Speak several utterances.
- Verify vertical scrollbar appears and scrolls correctly.
- Inspect Shadow DOM to ensure no leakage.
</verification>

<success_criteria>
- Reliable segmentation (TRNS-03).
- Responsive real-time display (TRNS-01).
- Visual isolation (Phase requirement).
</success_criteria>

<output>
After completion, create `.planning/phases/03-transcription-segmentation/03-03-SUMMARY.md`
</output>
